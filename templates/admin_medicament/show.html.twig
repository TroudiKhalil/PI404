{% extends 'admin.html.twig' %}

{% block title %}Medicament{% endblock %}

{% block body %}
    <h1>Medicament</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ medicament.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ medicament.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ medicament.description }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ medicament.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ medicament.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_admin_medicament_index') }}">back to list</a>

    <a href="{{ path('app_admin_medicament_edit', {'id': medicament.id}) }}">edit</a>

    {{ include('admin_medicament/_delete_form.html.twig') }}
{% endblock %}
